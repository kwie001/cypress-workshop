context("Übung zu Folien", () => {
    describe("Assertions auf Elementen", () => {
      beforeEach(() => {
        cy.loginViaRequest();
      });
  
      it("Should()", () => {
        
        
      });
    });
  });