context("Übung zu Folien", () => {
  describe("Assertion", () => {
    beforeEach(() => {
      cy.visit("https://example.cypress.io/todo");
    });

    it("Should()", () => {
      /** should() */

    });
  });
});